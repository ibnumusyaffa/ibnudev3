const { colors } = require("tailwindcss/defaultTheme")
module.exports = {
  theme: {
    colors: {
      black: colors.black,
      white: colors.white,
      gray: colors.gray,
      red: colors.red,
    },
    extend: {},
  },
  variants: {},
  plugins: [],
  corePlugins: {
    float: false,
  },
}
